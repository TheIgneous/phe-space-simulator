node_modules/
dist/
coverage/
playwright-report/
test-results/
.local-data/
*.xml
*.xlsx
*.local.xml
*.local.xlsx
.env
.env.*

# Shared timetable hub: these dummy-data exports are intentionally tracked so both
# apps read the same source (raw *.xml elsewhere stays ignored above).
!data/*.xml
